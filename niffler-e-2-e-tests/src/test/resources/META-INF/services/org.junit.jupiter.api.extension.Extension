guru.qa.niffler.jupiter.extension.ContextExtension
guru.qa.niffler.jupiter.extension.AllureLogsExtension
guru.qa.niffler.jupiter.extension.AllurePassedAttachmentsExtension

# Является частью механизма сервис-провайдеров в Java, который используется для регистрации расширений JUnit Jupiter (JUnit 5)
# Когда JUnit Jupiter загружает тесты, оно автоматически обнаруживает и регистрирует все расширения, перечисленные в этом файле.
# Вместо необходимости явно регистрировать расширения в тестовом коде (например, с помощью аннотации @ExtendWith), этот файл позволяет автоматически подключать расширения.